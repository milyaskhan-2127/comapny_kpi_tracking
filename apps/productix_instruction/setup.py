from setuptools import setup, find_packages

setup(
    name="productix_instruction",
    version="1.0.0",
    description="Productix Instruction Room module — in-app announcements and notifications",
    author="TechoHub",
    author_email="support@techohub.net",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)